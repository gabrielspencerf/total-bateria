# INST-2026-04-18 — Etapa 3 (medição + hardening)

## Implementado

- Script de evidência: execuções manuais de `npx lighthouse@11.7.1` documentadas; artefatos JSON + markdown em `docs/releases/evidence/etapa-3-2026-04-18/`.
- Ajustes de UI/a11y listados em `docs/releases/RELEASE-2026-04-18-etapa-3-evidencia.md`.
- Suporte a WebM opcional no hero institucional (`HeroSection` + `mediaRuntime`).

## Build / lint

- `npm run lint` e `npm run build` no institucional após mudanças.
- LP: `npm run lint` e build com env `VITE_LANDING_KEY=empilhadeiras` (e demais variáveis obrigatórias do `vite.config.ts`).

## Lições

- Evitar `--only-categories` com lista CSV nesta versão do Lighthouse — gerou `categories` vazio; usar execução completa ou flags conforme documentação da versão.
