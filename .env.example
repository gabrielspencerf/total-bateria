PORT=3000
NODE_ENV=development

# Obrigatórios para build/runtime (validados em vite.config.ts e config/runtime.ts)
# Copie para .env local e ajuste valores reais (não commitar segredos).
VITE_LANDING_KEY=baterias
VITE_SITE_URL=https://www.exemplo.com.br
VITE_WHATSAPP_NUMBER=00000000000
VITE_COMPANY_NAME=Total Bateria

# Opcional — subpasta pública (deve coincidir com deploy + React Router basename)
# VITE_BASE_PATH=/

# Opcional
# VITE_REGION_LABEL=
